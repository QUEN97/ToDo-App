import TodoApp from './components/todoApp';//automaticamente se importa al ser llamado dentro de la funcion

function App() {
  return <TodoApp />; // llamamos al componente
  
}

export default App;
